import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/column/08_gal')({
  component: Column08GalPage,
})

function Column08GalPage() {
  return <div>Column: Gal (TODO: implement)</div>
}
