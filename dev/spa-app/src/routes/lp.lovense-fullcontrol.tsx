import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/lp/lovense-fullcontrol')({
  component: LpLovenseFullcontrolPage,
})

function LpLovenseFullcontrolPage() {
  return <div>Lovense LP (TODO: implement)</div>
}
